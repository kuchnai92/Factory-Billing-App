name: Build Flet APK

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        # This pre-release version is the only one that reliably supports --icon on GitHub
        pip install flet --pre
        pip install "flet[cli]"
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Setup Android SDK Tools
      uses: android-actions/setup-android@v3
      with:
        cmdline-tools-version: 11076708

    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        channel: 'stable'

    - name: Accept Licenses
      run: |
        yes | sdkmanager --licenses || true
        yes | flutter doctor --android-licenses || true
        flutter doctor || true

    - name: Build APK
      run: |
        # We use the full path to the freshly installed flet tool
        python3 -m flet build apk --icon assets/icon.png --product "Factory Billing" --verbose

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: my-billing-app
        path: build/apk/*.apk
